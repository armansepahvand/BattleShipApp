const Player = require('../classes/Player');

test('Player default properties shuold  match the values ', () => {});

test('Check the seters and getters of the Player Class', () => {});

test('Check the shots filred by player ', () => {});
